Special Elite (Regular, Latin subset), by Astigmatic.

Licensed under the SIL Open Font License, 1.1 — https://openfontlicense.org
Fetched from Google Fonts (fonts.gstatic.com) and vendored here so the app
never loads fonts from a CDN at runtime, consistent with the CSP
(`default-src 'self'`) and the no-CDN-dependency principle in
docs/THREAT_MODEL.md.
